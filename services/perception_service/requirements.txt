# The perception service on its own, for a deployment that runs it apart from
# the Space image. Deliberately small: the half of the model that carries most
# of the value -- what a given pair of eyes can and cannot resolve on a real
# capture -- is arithmetic over pixels and needs no model at all.
fastapi>=0.115,<1
uvicorn>=0.30,<1
pydantic>=2.9,<3
pillow>=10.4,<12
numpy>=1.26,<3

# The optional screen parser, for finding what the DOM never declared -- text
# baked into a hero image, a control drawn on a canvas. torch plus weights is a
# large addition for one endpoint, so it is not installed by default and the
# service reports itself as running without it:
#
#   pip install torch ultralytics easyocr
#   export OMNIPARSER_WEIGHTS=/path/to/icon_detect/model.pt
